'use strict';

describe('zmReviewer.timeline module', function() {

  beforeEach(module('zmReviewer.timeline'));

  describe('timeline controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('timelineCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});